{
    "name": "tsPizza",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "start": "cd backend && node index.js",
      "build": "cd my-app && npm install && npm run build && cp -r build/* ../backend/public"
    },
    "dependencies": {}
  }
  